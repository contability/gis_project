<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.gitt.kworks.mappers.KwsCntmMapper">

	<!-- 전체 목록 검색 -->
	<select id="listAll" resultType="kwsCntm">
		SELECT
			CNTM_ID,
			CNTM_NM,
			CNTM_CN,
			CNTM_RM
		FROM
			KWS_CNTM
	</select>

</mapper>