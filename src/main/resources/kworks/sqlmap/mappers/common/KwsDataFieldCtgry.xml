<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.gitt.kworks.mappers.KwsDataFieldCtgryMapper">

	<!-- 목록 검색 -->
	<select id="listAll" resultType="kwsDataFieldCtgry">
		SELECT
			FIELD_CTGRY_ID,
			FIELD_CTGRY_NM,
			SORT_ORDR,
			USE_AT
		FROM
			KWS_DATA_FIELD_CTGRY
	</select>

</mapper>